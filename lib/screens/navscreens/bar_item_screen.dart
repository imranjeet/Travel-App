import 'package:flutter/material.dart';

class BarItemScreen extends StatelessWidget {
  const BarItemScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("Bar Item Screen"))
    );
  }
}